Aim: Create the RDS by using Terraform

#Files Used 
-provider.tf
-terraform.tfvars
-main.tf
-variables.tf
-output.tf

#terraform commands to compile the code
terraform init
terraform validate
terraform plan
terraform apply --auto-approve
terraform destroy --auto -approve
